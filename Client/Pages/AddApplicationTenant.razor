@page "/add-application-tenant"
@attribute [Authorize]
@using SmartNest.Client.Services
@inject Microsoft.Extensions.Localization.IStringLocalizer<AddApplicationTenant> L
@inject DialogService DialogService
@inject TenantService TenantService
@inject NotificationService NotificationService

<RadzenRow>
    <RadzenColumn SizeMD="12">
        <RadzenTemplateForm TItem="CreateTenantDto" Data="@tenant" Visible="@(tenant != null)" Submit="@FormSubmit">
            <RadzenAlert Title="Cannot create tenant" Shade="Shade.Lighter" Variant="Variant.Flat" Size="AlertSize.Small" AlertStyle="AlertStyle.Danger" Visible="@errorVisible">@error</RadzenAlert>

            <RadzenStack style="margin-bottom: 1rem;">
                <RadzenFormField Text="Name" Variant="Variant.Flat">
                    <ChildContent>
                        @if (tenant != null)
                        {
                            <RadzenTextBox style="width: 100%" @bind-Value="@(tenant.Name)" Name="Name" />
                        }
                    </ChildContent>
                    <Helper>
                        <RadzenRequiredValidator Component="Name" Text="Name is required" />
                    </Helper>
                </RadzenFormField>
                <RadzenFormField Text="Hosts" Variant="Variant.Flat">
                    <ChildContent>
                        @if (tenant != null)
                        {
                            <RadzenTextBox style="width: 100%" @bind-Value="@(tenant.Hosts)" Name="Hosts" />
                        }
                    </ChildContent>
                    <Helper>
                        <RadzenRequiredValidator Component="Hosts" Text="Hosts is required" />
                        <RadzenText TextStyle="TextStyle.Caption">Enter comma-separated hostnames (e.g., tenant1.example.com, tenant1.com)</RadzenText>
                    </Helper>
                </RadzenFormField>
            </RadzenStack>    
            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" JustifyContent="JustifyContent.End" Gap="0.5rem">
                    <RadzenButton ButtonType="ButtonType.Submit" Icon="save" Text="Save" Variant="Variant.Flat" IsBusy="@isBusy" />
                    <RadzenButton ButtonStyle="ButtonStyle.Light" Text="Cancel" Click="@CancelClick" Variant="Variant.Flat" />
            </RadzenStack>
        </RadzenTemplateForm>
    </RadzenColumn>
</RadzenRow>

@code {
    protected CreateTenantDto tenant = new();
    protected bool errorVisible;
    protected string? error;
    protected bool isBusy;

    protected override async Task OnInitializedAsync()
    {
        tenant = new CreateTenantDto();
    }

    protected async Task FormSubmit()
    {
        try
        {
            isBusy = true;
            errorVisible = false;
            
            var response = await TenantService.CreateTenantAsync(tenant);

            if (response.IsSuccessStatusCode)
            {
                NotificationService.Notify(new NotificationMessage 
                { 
                    Severity = NotificationSeverity.Success, 
                    Summary = "Success", 
                    Detail = "Tenant created successfully", 
                    Duration = 4000 
                });
                
                DialogService.Close(true);
            }
            else
            {
                errorVisible = true;
                var errorContent = await response.Content.ReadAsStringAsync();
                error = string.IsNullOrEmpty(errorContent) ? "An error occurred" : errorContent;
            }
        }
        catch (Exception ex)
        {
            errorVisible = true;
            error = ex.Message;
        }
        finally
        {
            isBusy = false;
        }
    }

    protected void CancelClick()
    {
        DialogService.Close(false);
    }
}